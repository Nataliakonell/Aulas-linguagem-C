#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
//1. O empreendimento “ToDoPão” vende uma certa quantidade de hambúrgueres e uma quantidade de empadas a cada dia. Cada hambúrguer custa R$ 25,00 e a empada custa R$ 12,50. Ao final do dia, o dono quer saber quanto arrecadou com a venda dos hambúrgueres e empadas (juntos), e quanto deve guardar numa conta  poupança (12% do total arrecadado). Faça um algoritmo para ler as quantidades de hambúrgueres e de empadas, e depois calcular os dados solicitados. 

int main()
{ 

//Declaração das variáveis
int qtd_hamb,qtd_emp;
float valor_hamb, valor_emp, total, poupança;

//Recebe as informações
printf("Informe a qtd de hamburguer: ");
scanf("%d",&qtd_hamb);
printf("Informe a qtd de empadas: ");
scanf("%d",&qtd_emp);

//Calcular venda
valor_hamb=qtd_hamb*25;
valor_empada=qtd_emp*12.5;
total=valor_hamb+valor_emp;
poupança=total*0.12;

//Exibir resultado vendido e o valor a guardar na poupança
printf("Total vendido: %.2f\n", total);
printf("Total a ser guardado: %.2f\n", poupança);

}
    return 0;
}
